ktor {
  deployment {
    port = 8080
    shutdownTimeout = 15000
  }
  application {
    modules = [no.nav.tsm.ApplicationKt.module]
  }
}

app {
  name = "local-pod"
}

external {
  hprExportEndpoint = "http://localhost:8818/hpr_export.zip"
}

postgres {
  jdbc = "jdbc:postgresql://localhost:7971/tsm-behandler"
  username = "user"
  password = "password"
  schema = "public"
}
